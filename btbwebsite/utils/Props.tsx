//Layout Page Props
export type LayoutProps = {
    children: React.ReactNode;
    title: string;
    description: string | undefined;
};

//Head element props
export type SeoProps = {
    title: string;
    description: string | undefined;
};

//Props for button component and variants
