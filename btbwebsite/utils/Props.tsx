export type LayoutProps = {
    children: React.ReactNode;
    title: string;
    description: string | undefined;
};
export type SeoProps = {
    title: string;
    description: string | undefined;
};